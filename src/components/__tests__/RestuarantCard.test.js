import { render } from "@testing-library/react";
import RestuarantCard from "../RestuarantCard";

it("should render RestuarantCard components with props Data", () => {
    render(<RestuarantCard/>)
}); 